[package]
name = "matcher"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
rand = "0.10.1"
wit-bindgen = { version = "*" }
wasi-otel-framework = { path = "../otel" }
